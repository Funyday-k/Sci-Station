[project]
name = "sci-station-agent"
version = "0.1.0"
description = "Local Sci-Station sidecar runtime for agent workflow orchestration."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8"]
uitest = ["pyyaml>=6"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
