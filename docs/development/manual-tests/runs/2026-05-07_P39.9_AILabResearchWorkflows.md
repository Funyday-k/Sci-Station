# MT07 P39.9 AI Lab Research Workflows

日期：2026-05-07

范围：AI Lab paper routing、structured tool payload、reasoning_content 回传、Chat/Assistant paper parity、draft-first write approval、tool error taxonomy、answer quality evaluator。

## 自动化验证

```text
swift run SciStationCoreTestRunner
结果：通过；All SciStation core checks passed.

/Users/funyday/Documents/Sci-Station/.venv/bin/python -m pytest AgentRuntime/tests
结果：通过；28 passed.

xcodebuild -project Sci-Station.xcodeproj -scheme Sci-Station -destination 'platform=macOS' build
结果：通过；BUILD SUCCEEDED.
```

## 覆盖结论

```text
reasoning_content thinking-mode 回传：已由 OpenAI-compatible provider payload/parse 回归覆盖。
deterministic paper preflight：已由 Swift loop list_papers -> search_papers -> read_paper_section 回归覆盖。
structured payload：已由 list/search/read paper tool payload 回归覆盖。
answer quality：已覆盖 direct/source/display math/missing evidence 检查。
AgentRuntime parity：已覆盖 selected-paper formula prompt 路由到 paper_reading。
draft-first approval：已覆盖 write_markdown_plan 审批预览的 target/diff/rollback 代码路径，GUI live 待复核。
```

## 手测矩阵

```text
MT07-P39.9-01: Chat / 列论文 -> id/title/path 可见
状态：待 live GUI 复核；自动化覆盖 list_papers structured payload。

MT07-P39.9-02: Assistant / 列论文 -> 与 Chat parity
状态：待 live GUI 复核；自动化覆盖 Assistant plan backfill 代码路径。

MT07-P39.9-03: Chat / 第一篇公式 -> direct answer + source
状态：待 live provider 复核；自动化覆盖 deterministic preflight + display math/source quality。

MT07-P39.9-04: Assistant / 第一篇公式 -> 与 Chat parity 或明确模式限制
状态：待 live GUI 复核；自动化覆盖 sidecar/Swift routing 不落 general。

MT07-P39.9-05: 缺 Markdown 论文 -> 不编造，提示转换/导入
状态：待 live GUI 复核；自动化覆盖 missing evidence/missing conversion 文案路径。

MT07-P39.9-06: 总结并写入 wiki -> 草稿 -> 审批 -> 写入路径 -> 成功回显
状态：待 live GUI 复核；自动化覆盖审批 target/diff/rollback 和 write result payload。

MT07-P39.9-07: 拒绝写入 -> 保留草稿，timeline 可解释
状态：待 live GUI 复核；依赖 Permission Dock 交互，P39.10 会继续视觉收口。

MT07-P39.9-08: section not found -> 展示搜索路径和候选
状态：待 live GUI 复核；自动化覆盖 section_not_found taxonomy 与建议文案。
```

## 剩余风险

```text
1. 真实 thinking-mode provider 需要用用户实际 API key 复测一次多轮工具调用，确认服务端接受 reasoning_content 回传。
2. 当前 UI 仍使用 legacy markdown renderer，公式显示质量留给 P39.10。
3. Permission Dock 的产品化语义已增强，但视觉整合和审批完成后的 compact audit row 留给 P39.10。
```