# P39.5 AI Lab Stabilization Manual Test Record

Date: 2026-05-06
Task book: `docs/development/Proposal39.5.md`
Tester: Copilot implementation pass

## Build / Automation

- `swift run SciStationCoreTestRunner`: PASS
- `/Users/funyday/Documents/Sci-Station/.venv/bin/python -m pytest AgentRuntime/tests`: PASS, 28 passed
- `xcodebuild -project Sci-Station.xcodeproj -scheme Sci-Station -destination 'platform=macOS' build`: PASS
- `get_errors` for edited Swift/App files: PASS, no diagnostics reported

## P39.5 Focus Cases

| ID | Status | Notes |
|---|---|---|
| MT07-P39.5-01 | Covered by automation / needs GUI spot check | `AgentRun` and `AgentThread` now persist `context_scope`, `project_id`, `runtime_selector`, `created_from_route`; pending New Chat remains in memory until run attach. |
| MT07-P39.5-02 | Covered by code review / needs GUI spot check | AI Lab context picker now controls next-run context; sidebar project focus no longer resets an active/pending thread. |
| MT07-P39.5-03 | Covered by code review / needs GUI spot check | Draft key now prefers active/pending thread affinity instead of mutable next-run context. |
| MT07-P39.5-04 | Covered by automation | `recordFailedRun` persists user message plus inline `tool_call_failed` timeline event. |
| MT07-P39.5-05 | Partially covered / needs GUI spot check | Stop records a failed/stopped run with partial assistant text when available. |
| MT07-P39.5-06 | Covered by repository behavior / needs GUI spot check | Archived threads remain readable but are hidden from active list; active thread resets after archive. |
| MT07-P39.5-07 | Covered by state review / needs GUI spot check | No active thread falls through to empty New Chat timeline instead of archived content. |
| MT07-P39.5-08 | Needs GUI IME check | Composer now uses `NSTextView` bridge and does not submit while `hasMarkedText()` is true. |
| MT07-P39.5-09 | Needs GUI spot check | Tool picker is a checkbox popover with Select All / Clear and does not close on individual toggles. |
| MT07-P39.5-10 | Covered by automation | Read-only tools classify as auto-allowed and are filtered out of Permission Dock approval items. |
| MT07-P39.5-11 | Needs sidecar/model manual check | Write workflows still produce write-tool approval items; read/summarize phase remains read-only. |
| MT07-P39.5-12 | Covered by code path / needs manual run | Approve/resume now uses run metadata context and preserves inline error on thrown failures. |
| MT07-P39.5-13 | Needs GUI spot check | Assistant bubbles include copy action using Markdown/plain text content. |
| MT07-P39.5-14 | Covered by automation | Run metadata snapshots enabled tools; Permission Dock uses run snapshot before current UI selection. |

## Manual Execution Limitation

This pass did not launch and click through the macOS UI. The table above separates automation/code-path coverage from GUI checks that still require an interactive app session, especially Chinese IME composition and popover behavior.

## Known Issues

- Full LangGraph/provider-backed workflows still require manual validation with configured credentials and sidecar availability.
- GUI-only visual checks remain pending until an interactive macOS app run.
