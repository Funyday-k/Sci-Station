# P39.15 / P39.16 Manual Gate：AI Lab Markdown / Wiki Writeback / Budget

日期：2026-05-07
状态：自动化 PASS / GUI 回放待执行

## 自动化结果

```text
swift run SciStationCoreTestRunner
=> All SciStation core checks passed.
```

## Code-Level Checks

- MT07-P39.15-01: Assistant rich Markdown remains routed through local `ChatRenderer.bundle`.
- MT07-P39.15-02: Markdown preview uses local `marked.min.js`, `dompurify.min.js`, KaTeX, and auto-render assets.
- MT07-P39.15-03: `write_markdown_plan` / `write_wiki_markdown` expose target path and draft body in Permission Dock.
- MT07-P39.15-04: Permission Dock now presents approve/write, draft-only, and deny actions.
- MT07-P39.15-05: Draft-only action writes `wiki/drafts/<slug>.draft.md` and does not resume the pending tool call.
- MT07-P39.15-06: Successful writeback events expose a workspace-relative open action from `modified_paths` or `target_path`.
- MT07-P39.15-07: Continuation prompts inject compact previous tool evidence into the next LLM context.
- MT07-P39.15-08: Provider fallback/empty-response handling remains in the loop path.

## ResearchWorkspace GUI Replay

Run these in the ResearchWorkspace AI Lab after building the app:

```text
MT99-P39.15-01: evaporation rate?
MT99-P39.15-02: 继续
MT99-P39.15-03: 写进wiki里
MT99-P39.15-04: 对这个文章做一个总结放进wiki里
MT99-P39.15-05: 第三篇文章里的蒸发率公式是什么？
```

Expected result: at least 4/5 complete with a normal answer, an approval pause, or a visible fallback rather than a silent/malformed failure.

## Pending Manual Notes

- Record actual GUI replay outcomes here once the app is launched.
- Confirm approved `wiki/papers/<id>.md` writeback opens the same page as Generate Wiki Page.
- Confirm offline preview has no CDN/jsdelivr requests.
