# P39.6 AI Lab Full Flow Manual Test Record

Date: 2026-05-07
Task book: `docs/development/Proposal39.6.md`
Tester: Copilot implementation pass

## Build / Automation

- `get_errors` for edited Agent/App/Test files: PASS, no diagnostics reported
- `swift run SciStationCoreTestRunner`: PASS
- `/Users/funyday/Documents/Sci-Station/.venv/bin/python -m pytest AgentRuntime/tests`: PASS, 28 passed
- `xcodebuild -project Sci-Station.xcodeproj -scheme Sci-Station -destination 'platform=macOS' build`: PASS

## P39.6 Focus Cases

| ID | Status | Notes |
|---|---|---|
| MT07-P39.6-01 | Covered by implementation / needs live provider GUI check | Provider-native tool schema normalization remains covered; ordinary chat should no longer fail because legacy tool schemas lack `type: object`. |
| MT07-P39.6-02 | Covered by automation / needs live provider GUI check | Planner readable fallback is covered by core tests; readable non-JSON answers are surfaced instead of appended as structure errors. |
| MT07-P39.6-03 | Covered by automation / needs live provider GUI check | Prompt and SwiftLoop tests now cover first-paper formula flow: `list_papers -> search_papers -> read_paper_section -> final answer` with formula/source requirements. |
| MT07-P39.6-04 | Covered by automation | Assistant/Plan readable fallback remains covered by `SciStationCoreTestRunner`. |
| MT07-P39.6-05 | Covered by fixture automation / needs sidecar live check | LangGraph golden fixtures still replay final response, artifact, and approval resume events. |
| MT07-P39.6-06 | Covered by automation / needs sidecar live check | Sidecar unavailable timeout fixture emits fallback events; runtime selection fallback remains in SwiftLoop path. |
| MT07-P39.6-07 | Covered by automation / needs live provider GUI check | Auto/LangGraph parity is covered at fixture level; real provider comparison remains pending. |
| MT07-P39.6-08 | Covered by automation / needs GUI approval click check | Write tools still pause through Permission Dock and resume through approved execution tests. |
| MT07-P39.6-09 | Covered by automation | Read-only tools now append compact timeline summaries such as `已使用工具：read_paper_section`; auto-allow approval noise is still filtered from the dock. |
| MT07-P39.6-10 | Covered by code review / needs GUI spot check | Assistant copy button remains at the bubble footer and copies Markdown/plain text content. |
| MT07-P39.6-11 | Covered by code review / needs GUI spot check | AI Lab context picker uses `运行范围` and `全工作区`, avoiding the misleading `Workspace` label. |
| MT07-P39.6-12 | Covered by automation | Read-only tool success followed by provider empty response now returns a visible inline fallback with model, failure reason, used tools, and last tool result summary. |

## New Regression Coverage Added

- `agentLoopRunnerReturnsVisibleFallbackAfterToolThenEmptyProvider`: verifies provider empty response after a successful read-only tool becomes a visible assistant fallback and preserves the last tool summary.
- `agentLoopRunnerPaperFormulaFlowUsesListSearchReadBeforeFinal`: verifies list/search/read tool sequencing and final Markdown formula/source output.
- `agentPromptBuilderDirectsPaperToolsForMetadataOnlyContext`: extended to require ordinal-paper and formula guidance in both planner and tool-loop prompts.

## Manual Execution Limitation

This pass did not launch and click through the macOS UI with a real DeepSeek/OpenAI-compatible credential. The implementation now has automated coverage for the previously blocking runtime/tool-loop paths, but the live provider matrix remains a GUI/manual validation item for P39.7.

## Known Risks

- Real model behavior may still vary on first-paper formula questions; P39.7 should capture screenshots and actual prompt/output transcripts for SwiftLoop, LangGraph, and Auto fallback.
- Sidecar live health and crash behavior were validated through fixtures, not a long-running sidecar process in an interactive app session.
- GUI-only copy button placement and context-picker wording were reviewed in code and build, but still need a visual spot check.
