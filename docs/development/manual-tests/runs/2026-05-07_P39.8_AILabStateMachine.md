# P39.8 AI Lab State Machine Manual Test Record

Date: 2026-05-07
Task book: `docs/development/Proposal39.8.md`
Tester: Codex implementation pass

## Build / Automation

- `swift run SciStationCoreTestRunner`: PASS
- `/Users/funyday/Documents/Sci-Station/.venv/bin/python -m pytest AgentRuntime/tests`: PASS, 28 passed
- `xcodebuild -project Sci-Station.xcodeproj -scheme Sci-Station -destination 'platform=macOS' build`: PASS

## Implementation Notes

- `AgentRun` now persists `lifecycle_state`, `failure_category`, and `retry_of_run_id` while decoding older JSONL runs by inferring lifecycle state.
- Failed and cancelled runs are retryable through a first-class retry action.
- Cancelled runs now persist a dedicated `run_cancelled` session event instead of looking like generic provider failures.
- Timeline rendering can project legacy runs that have no session events into user/assistant/failure/approval timeline items.
- Retry keeps the source run id and uses the original prompt/context snapshot path for the next run.

## P39.8 Focus Cases

| ID | Status | Notes |
|---|---|---|
| MT07-P39.8-01 | Covered by automation | `recordFailedRun` persists lifecycle/failure metadata and timeline events. |
| MT07-P39.8-02 | Covered by automation/code path | Retry source metadata round-trips; GUI click spot check remains pending. |
| MT07-P39.8-03 | Covered by existing P39.6 automation | Provider empty after tool read remains visible and retryable. |
| MT07-P39.8-04 | Partially covered | Pending approval state remains run/thread-scoped; live thread-switch click check remains pending. |
| MT07-P39.8-05 | Covered by automation | `recordCancelledRun` persists partial assistant text and `run_cancelled` event. |
| MT07-P39.8-06 | Covered by projection regression | Legacy runs without events project into timeline after reload. |
| MT07-P39.8-07 | Deferred GUI check | Archived thread replay and dock suppression need interactive UI pass. |

## Known GUI Follow-Up

- Click retry on an actual failed bubble and verify a new run appears with old run preserved.
- Trigger a write-tool approval, switch threads, and verify the approval card does not leak.
- Archive a thread with old failed/cancelled runs and verify replay remains read-only.
