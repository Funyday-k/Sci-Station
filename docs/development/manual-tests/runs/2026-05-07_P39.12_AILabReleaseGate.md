# Manual Test Run: P39.12 AI Lab Release Gate

测试日期：2026-05-07
测试人：GitHub Copilot 预填，等待用户在 macOS App 中实测确认
任务书编号：P39.12
测试 workspace：`/Users/funyday/Documents/ResearchWorkspace`
结论：CONDITIONAL PASS，自动化与 Xcode build 已通过；人工 UI 操作待用户在 macOS App 中确认。

## Scope

```text
MT07-P39.12-01: AI Lab selected source health for library/papers
MT07-P39.12-02: chunks=0 / not indexable source hint and actions
MT07-P39.12-03: Library inspector paper.md Health
MT07-P39.12-04: Provider failure fallback actions
MT07-P39.12-05: Copy diagnostic redaction
MT07-P39.12-06: Figure 2 / 图 2 paper tool reading
MT99-P39.12-01..04: Release regression gate
```

## Log Findings Driving This Run

- DeepSeek thinking-mode runs failed with HTTP 400 because assistant tool-call replay did not preserve the required `reasoning_content` contract for `deepseek-v4-flash`.
- Paper QA runs also stopped when accumulated tool evidence exceeded the local context/tool-result budget; useful evidence had already been read, but the UI did not surface a final fallback clearly enough.
- Figure 2 requests can be misrouted through `read_paper_section` as a heading lookup even though `Figure 2` is a caption, not a Markdown heading.
- Wiki writeback failed when model output contained a JSON object plus extra prose; the parser needed balanced-object extraction instead of first-brace-to-last-brace slicing.

## Automation

```text
swift run SciStationCoreTestRunner: PASS
xcodebuild -project Sci-Station.xcodeproj -scheme Sci-Station -destination 'platform=macOS' build: PASS, with known ChatMarkdownWebView actor-isolation warnings
```

## Manual Checklist

```text
[ ] Open AI Lab and select Garani / dark-matter-sun paper.
[ ] Source Health shows library/papers/.../paper.md, chunks, and paper.md health.
[ ] Check paper.md updates the health summary and issue list.
[ ] Open paper.md opens the selected paper markdown.
[ ] Ask about Figure 2 / 图 2 and confirm tool evidence includes Figure 2 caption lines and local image refs.
[ ] Force or observe provider failure and confirm Retry / Copy Diagnostic / Tool Evidence actions are visible.
[ ] Copy Diagnostic and verify no API key/token/full home absolute path is present.
[ ] Run MT99 partial regression items for AI Lab, Library Inspector, Settings, and secret scan.
```

## Residual Risks

- Manual UI validation is still required because automated tests do not instantiate the full SwiftUI inspector and AI Lab panels.
- `ChatMarkdownWebView` WebKit actor-isolation warning remains a tracked release risk outside P39.12 unless it becomes a runtime failure.